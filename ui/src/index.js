/*

1. Image at the top
    a. occupies full width and 35% height
2. Name/contact right below the image
3. two buttons side by side(Email and Call) 
4. Two sections
    a. About
    b. Interests
*/


import react from 'react';
import ReactDOM from 'react-dom';

import App from './App';

import './index.css';
import './css/styles.css';

const root = ReactDOM.createRoot(document.getElementById('root'))


root.render(<App />, root)